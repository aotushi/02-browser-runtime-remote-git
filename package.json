{
  "name": "site-10-layer-check-browser-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "site-check-browser": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js",
    "dev": "npm run build && node dist/cli.js",
    "probe:tls": "node tools/live-tls-probe.mjs",
    "probe:lighthouse": "node tools/lighthouse-probe.mjs"
  },
  "dependencies": {
    "lighthouse": "^13.3.0",
    "playwright": "^1.53.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
